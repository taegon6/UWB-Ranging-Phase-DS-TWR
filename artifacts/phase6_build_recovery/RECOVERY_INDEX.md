# Phase 6.1 recovery artifact index

`build_attempt_manifest.json` in this directory is the preserved first reproduction of the incorrect `-batch` invocation. It is not the authoritative compile attempt.

The authoritative direct-project invocation, complete stdout/stderr build log, empty-output inventory, and failure manifest are in `../phase6_build_recovery_attempt03/`.

source_type = CODE_INSPECTION_AND_BUILD
hardware_verified = false
flash_executed = false
